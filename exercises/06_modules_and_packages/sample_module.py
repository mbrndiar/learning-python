"""
Fixture module for Chapter 6 exercises.

This file is never run directly -- it exists only to be imported by
exercises.py, so the exercise checks can inspect a real module's __name__
after an ordinary import (which is never "__main__").
"""

executed_as_script = __name__ == "__main__"
